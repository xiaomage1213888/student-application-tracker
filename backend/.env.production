# 生产环境配置
PORT=3000
NODE_ENV=production

# JWT 配置（请修改为你自己的密钥）
JWT_SECRET=student-tracker-prod-secret-key-2024
JWT_EXPIRES_IN=7d

# 数据库配置（使用 SQLite，简单可靠）
DB_DIALECT=sqlite
DB_STORAGE=./data/database.sqlite

# 文件上传配置
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760
